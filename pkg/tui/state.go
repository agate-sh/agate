package tui

// UIMode represents the different modes the TUI can be in
type UIMode int

// UIState represents the current state of the UI
type UIState struct {
	// State fields will be added in Phase 2
}
